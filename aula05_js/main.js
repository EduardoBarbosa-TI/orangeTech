const {gets, print} = require("./script.js");

print(gets());
print(gets());
print(gets());
print(gets());
print(gets());
print(gets());




const eduardo = {
    nome:'eduardo'
};

// const { nome } = pessoa;
const nome = pessoa.nome;

print(gets());